- lire tout LD_LIBRARY_PATH, le mettre dans une structure, en liste chaine
- chercher dans chaque PATH si les libs sont dedans
- refaire structure complete en enlevant name et link_map de l'elf, struct context ? 
- fonction get_dyn_num()
- refaire ldd a la bien avec la recursion l'adresse tout ca
